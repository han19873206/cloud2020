server:
  port: 80
#服务名称
spring:
  application:
    name: cloud-order-service
#配置Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka/